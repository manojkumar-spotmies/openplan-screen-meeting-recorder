{
  "name": "openplan-recorder-monorepo",
  "version": "1.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "@types/dom-mediacapture-record": "^1.0.22",
    "@webref/idl": "^3.82.1",
    "fake-indexeddb": "^6.0.0",
    "happy-dom": "^14.12.0",
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  }
}
